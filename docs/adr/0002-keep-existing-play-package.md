# Keep existing Play package

The Publishing Test keeps the existing Play Package `com.inaya.athenaspacequest` because an app record for that package already exists in Google Play Console. Renaming the package would create a different app identity and would not test the existing Play Console path.
